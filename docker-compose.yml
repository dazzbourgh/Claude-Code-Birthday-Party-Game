version: '3.8'

services:
  claude-party-game:
    build: .
    restart: unless-stopped
    container_name: claude-party-game
    networks:
      - lab_network

networks:
  lab_network:
    external: true
